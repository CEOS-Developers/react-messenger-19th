import styled from 'styled-components';

const StyledProfileContent = styled.div`
  flex-grow: 1;
  margin-top: 16px;
`;

export default function ProfileContent() {
  return <StyledProfileContent>hi</StyledProfileContent>;
}
